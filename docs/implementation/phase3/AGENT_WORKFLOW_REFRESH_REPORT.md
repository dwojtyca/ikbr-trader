# Agent instructions refresh — report

Independent plan and document reviews: ACCEPT by different reviewers.

AGENTS.md now records the owner's main/commit/push/exact-CI workflow, independent
plan and implementation review loops, preservation of unrelated work and current
local checks. Prose-only changes use local factual/link/diff checks rather than
repeated runtime suites; existing-runbook operational settings are distinguished
from repository implementations.

Current architecture references and PKO scope are documented with dated preflight
evidence. Strategy activation remains authoritative in source/configuration rather
than a stale duplicated list. Operational authorization is distinguished from
technical capability; supported closes retain ownership, deterministic risk,
audit and idempotency without inventing a new strategy entry. Existing safety
rules, including no IBKR UI automation and no direct live submission, remain.

Validation: all AGENTS.md relative links resolve; git diff --check passes; all29
unrelated research files retain their baseline hashes. Reviewed/staged scope is
three Markdown files only. No runtime code, local settings, broker orders or
positions changed by this documentation update. Owner will close SMR manually.
Commit/push and exact-commit GitHub CI verification follow this report.
